---
permalink: /llms-full.txt
eleventyExcludeFromCollections: true
---
{#- Once a 55 KB dump of every collection's variables and assets, all of it
    copied from STAC. That is what STAC is for, so this URL now serves the
    same file as /llms.txt and exists only because links to it are out in the
    wild (robots.txt, third parties). -#}
{% include "llms-body.njk" %}
