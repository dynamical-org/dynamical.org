---
layout: base.njk
title: catalog
---

<div class="content">
  <div style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap;">
    <h1>Catalog</h1>
    <div style="margin-left: auto; display: flex; gap: 1rem; align-items: center;">
      <span><a href="https://stac.dynamical.org/catalog.json">STAC</a> (<a href="https://radiantearth.github.io/stac-browser/#/external/stac.dynamical.org/catalog.json">browse</a>)</span>
    </div>
  </div>

  {% macro catalogTable(entries, isForecast) %}
    <div class="table-container">
      <table class="data catalog-list">
        <tr>
          <th>Model</th>
          <th>Domain</th>
          <th>Resolution</th>
          <th>{{ "Init cadence" if isForecast else "Time step" }}</th>
          <th>{{ "Lead time" if isForecast else "Record" }}</th>
          <th>Optimized for</th>
        </tr>
        {% for entry in entries %}
          <tr>
            <td>
              {% if entry.status == 'live' or entry.status == 'available' %}
                <a href="/catalog/{{ entry.id | slugify }}">{{ entry.model_name }}</a>
              {% else %}
                {{ entry.model_name }} ({{ entry.status }})
              {% endif %}
              <div class="cat-desc">{{ entry.description }}</div>
            </td>
            <td>{{ entry.spatial_domain }}</td>
            <td>{{ entry.spatial_resolution }}</td>
            {% if isForecast %}
              <td>{{ entry.time_resolution | replace("Forecasts initialized ", "") }}</td>
              <td>{{ entry.forecast_domain | replace("Forecast lead time ", "") | replace(" ahead", "") }}</td>
            {% else %}
              <td>{{ entry.time_resolution }}</td>
              <td>{{ entry.time_domain }}</td>
            {% endif %}
            <td>
              <span class="opt-badge opt-badge--{{ entry.optimization }}">{{ "space-optimized" if entry.optimization == "space" else "time-optimized" }}</span>
            </td>
          </tr>
        {% endfor %}
      </table>
    </div>
  {% endmacro %}

  <h2>Forecasts</h2>
  <p class="cat-intro">Archives of past and present model runs, identified by initialization time and stepping forward across lead times.</p>
  {{ catalogTable(catalog.forecasts, true) }}

  <h2 style="margin-top: 4rem;">Analyses</h2>
  <p class="cat-intro">Best-estimate archives of past weather, built by concatenating the earliest hours of each historical run into a continuous time series.</p>
  {{ catalogTable(catalog.analyses, false) }}

  <hr style="margin: 6rem 0;" />
  <h2>Roadmap</h2>
  <p>To view work in progress and upcoming roadmap, check out the <a href="https://github.com/orgs/dynamical-org/projects/1">dynamical.org Github project</a>.</p>

  <hr style="margin: 6rem 0;" />
  <h2>Experimental</h2>
  <div class="table-container">
    <table class="data catalog-table">
      <tr>
        <td><a href="/catalog/asos-parquet">Global Airport Observations (ASOS/AWOS)</a></td>
        <td>Historical and near-real-time METAR observations from ~2,500 stations. Updated hourly in GeoParquet.</td>
      </tr>
    </table>
  </div>

</div>
